{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run hellow.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/hellow.exe",
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "C/C++: gcc.exe build active file"
        }
    ]
}
